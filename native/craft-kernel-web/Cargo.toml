[package]
name = "frozen-rabbit-craft-kernel-web"
version = "0.1.0"
edition = "2024"
publish = false
description = "Versioned WebAssembly boundary for the Frozen Rabbit Rust craft kernel"
license = "MIT"
repository = "https://github.com/emu-rabbit/frozen_rabbit_cosmic"

[lib]
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
frozen-rabbit-craft-kernel = { path = "../craft-kernel" }
